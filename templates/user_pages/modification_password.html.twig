<h1 class="border-bottom">Modification de Mot de passe</h1>

<form method="POST" action="{{ path('user_edit', {'id': user.id}) }}">
    <div class="col-6 bg-light" style="padding:3%; border-radius:7%">
        {# <div class="row">
            <p class="col-6">Actuel<p>
            {{ form_widget(form.password_user.first) }}
        </div> #}
        <div class="row">
            <p class="col-6">{{ form_label(form.password.first) }}</p>
            <p>{{ form_widget(form.password.first) }}</p>
        </div>
        <div class="row">
            <p class="col-6">{{ form_label(form.password.second) }}</p>
            <p>{{ form_widget(form.password.second) }}</p> 
            <p class="text-alert">{{ form_errors(form.password.second) }}</p>          
        </div>

        <div class="d-flex justify-content-center">
            <button class="btn btn-light border border-dark">{{ button_label|default('Sauvegarder') }}</button>  
        </div>
    </div>
<form>